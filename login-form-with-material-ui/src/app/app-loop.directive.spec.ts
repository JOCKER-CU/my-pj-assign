import { AppLoopDirective } from './app-loop.directive';

describe('AppLoopDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLoopDirective();
    expect(directive).toBeTruthy();
  });
});
